const log = []

node main() {
  for (i in range(4)) {
    if (i == 0) {
      log.push("a")
    } else if (i == 2) {
      const y = interrupt("i is 2")
      log.push(y)
    } else {
      log.push("x")
    }
  }
  return log
}