node main(message: string) {
  mood: "happy" | "sad" = llm("Categorize the mood: ${message}")
  confidence: "high" | "low" = llm("How confident are you in that classification? ${mood}")
  return { mood: mood, confidence: confidence }
}
