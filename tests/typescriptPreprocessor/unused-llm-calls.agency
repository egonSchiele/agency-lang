node main() {
  // This prompt is assigned and used - should be kept
  used: string = llm("Give me a greeting")
  print(used)

  // This prompt is assigned but never used - should be removed
  unused: string = llm("Give me a farewell")

  // This prompt is returned - should be kept
  return llm("Final answer")
}
