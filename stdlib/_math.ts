export function _add(a: number, b: number): number {
  return a + b;
}

export function _subtract(a: number, b: number): number {
  return a - b;
}

export function _multiply(a: number, b: number): number {
  return a * b;
}
