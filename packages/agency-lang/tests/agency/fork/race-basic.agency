def identity(n: number): number {
  return n
}

node main() {
  const arr = [10, 20, 30]
  let result = race(arr) as item {
    return identity(item)
  }
  if (arr.includes(result)) {
    return true
  }
  return false
}