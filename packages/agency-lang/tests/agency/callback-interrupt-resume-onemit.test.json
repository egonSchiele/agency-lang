{
  "tests": [
    {
      "nodeName": "main",
      "description": "callback raising an interrupt from onEmit halts the surrounding node, surfaces the interrupt, and resumes after approve. beforeInterrupt == 1 (the substep before the interrupt does NOT re-run on resume), afterInterrupt == 1 (the substep after the interrupt fires once on resume), afterEmit == 1 (main continues past emit).",
      "input": "",
      "expectedOutput": "[1,1,1]",
      "evaluationCriteria": [
        {
          "type": "exact"
        }
      ],
      "interruptHandlers": [
        {
          "action": "approve",
          "expectedMessage": "pause"
        }
      ]
    }
  ]
}
