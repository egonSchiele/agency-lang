{
  "tests": [
    {
      "nodeName": "main",
      "description": "Two fork branches each mutate `counter` (their own per-branch copy) before, between, and after two interrupts. After both cycles, each branch's final counter should be 6 (1+2+3), proving global mutations from earlier in the branch survive multiple resume cycles.",
      "input": "",
      "expectedOutput": "[6,6]",
      "evaluationCriteria": [{ "type": "exact" }],
      "interruptHandlers": [
        { "action": "approve", "expectedMessage": "first a?" },
        { "action": "approve", "expectedMessage": "first b?" },
        { "action": "approve", "expectedMessage": "second a?" },
        { "action": "approve", "expectedMessage": "second b?" }
      ]
    }
  ]
}
