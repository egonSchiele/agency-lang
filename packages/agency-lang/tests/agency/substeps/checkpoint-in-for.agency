import { getMutable, setMutable } from "../../helpers/mutableVar.js"

node main() {
  const cp = checkpoint()
  setMutable("log", getMutable("log", "") + "start,")
  for (i in range(3)) {
    setMutable("log", getMutable("log", "") + i)
  }
  if (getMutable("log", "") == "start,012") {
    restore(cp, {})
  }
  return getMutable("log", "")
}
