class Counter {
  value: number

  constructor(start: number) {
    this.value = start
  }

  increment(): number {
    this.value = this.value + 1
    return this.value
  }
}

node main() {
  let c = new Counter(0)
  c.increment()
  return c.increment()
}
