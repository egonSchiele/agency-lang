import { add, greet } from "./utils.agency"

node testFunctionImport() {
  const result: number = llm("Use the add tool to add 3 and 4", { tools: [add] })
  return result
}

node testNodeImport() {
  return greet("Alice")
}
