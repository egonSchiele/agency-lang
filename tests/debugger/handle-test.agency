def greet(name: string): string {
  return interrupt("Confirm greet ${name}?")
  return "Hello, ${name}!"
}

node main() {
  const name = "Alice"
  handle {
    const greeting = greet(name)
    print(greeting)
  } with (data) {
    return approve()
  }
  return name
}
