export def mogrify(a: number, b: number): number {
  const sum = a + b
  return sum * 3
}

node withUses() {
  const result: number = llm("Use the mogrify function to mogrify 3, 7. Return ONLY the result.", { tools: [mogrify] })
  return result
}