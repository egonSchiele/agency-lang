let globalVar = "hello"

def helper(param) {
  const localVar = "world"
  const result = "${globalVar} ${localVar} ${param}"
  return result
}

node main() {
  const nodeLocal = "test"
  const output = helper(nodeLocal)
  print(output)
}
