{
  "tests": [{
    "nodeName": "main",
    "input": "",
    "expectedOutput": "3",
    "evaluationCriteria": [{ "type": "exact" }],
    "description": "callback registered inside a handle block shares the enclosing node's frame (like if/while blocks)"
  }]
}
