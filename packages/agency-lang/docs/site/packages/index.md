# Packages
This section lists some packages available for agency. All packages are hosted on NPM, so you can install them like any other NPM package.

For example:

```bash
npm install @agency-lang/mcp
```