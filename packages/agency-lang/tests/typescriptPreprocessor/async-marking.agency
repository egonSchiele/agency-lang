def pureFunction(x) {
  return x
}

def llmFunction(prompt) {
  const result = llm("Respond to: ${prompt}")
  return result
}

node main() {
  const a = pureFunction("hello")
  const b = llmFunction("world")
  const result: string = llm("Combine ${a} and ${b}")
  print(result)
}
