def foo(val: string) {
  return interrupt(val)
  return "got:${val}"
}

node main() {
  const results = []
  for (item in ["a", "b", "c"]) {
    const result = foo(item) with approve
    results.push(result)
  }
  return results
}
