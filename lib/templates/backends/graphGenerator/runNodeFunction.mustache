export async function {{{nodeName:string}}}({{{argsStr:string}}}): Promise<{{{returnType:string}}}> {
  const data = { {{{argsStr}}} };
  const result = await graph.run("{{{nodeName:string}}}", { messages: [], data });
  return result.data;
}
