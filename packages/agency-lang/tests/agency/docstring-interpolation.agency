const toolVersion = "2.0"

def versionedGreet(name: string): string {
  """
  Greets someone. Tool version: ${toolVersion}.
  """
  return "Hello, ${name}"
}

node main() {
  return versionedGreet("Alice")
}
