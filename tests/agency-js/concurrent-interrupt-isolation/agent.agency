globalVar = "unchanged"

node foo(sec: number, newGlobalVar: string | null) {
  if (newGlobalVar) {
    globalVar = newGlobalVar
  }
  confirmation = interrupt("confirm")
  sleep(sec)
  return globalVar
}
