safe def join(...parts: string[]): string {
  let result = ""
  for (part in parts) {
    if (result != "") {
      result = result + ", "
    }
    result = result + part
  }
  return result
}

node main() {
  let joined = join("a", "b", "c")
  return joined
}