const log = []

def record(val: number) {
  sleep(10ms)
  log.push(val)
}

def compute(val: number): number {
  sleep(10ms)
  return val * 2
}

node main() {
  const results = []
  let i = 0
  while (i < 3) {
    const x = async compute(i)
    results.push(x)
    async record(i)
    i = i + 1
  }
  return { results: results, log: log }
}
