def f(label: string): string {
  return "v-${label}"
}

node main() {
  parallel {
    let outerA = f("oa")
    seq {
      let forked = fork(["x", "y", "z"]) as item {
        return f(item)
      }
    }
  }
  return "${outerA}|${forked[0]},${forked[1]},${forked[2]}"
}
