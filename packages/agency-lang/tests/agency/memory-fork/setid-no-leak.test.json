{
  "tests": [
    {
      "nodeName": "main",
      "description": "setMemoryId inside a branch does not leak to the parent after join",
      "input": "",
      "expectedOutput": "\"base\"",
      "evaluationCriteria": [{ "type": "exact" }]
    }
  ]
}
