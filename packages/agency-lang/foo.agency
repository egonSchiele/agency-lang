import { search } from "std::wikipedia"

def callMe(block) {
  return block()
}

node main() {
  const n = 2
  const size = match(n) {
    100 => "big"
    2 => fork(range(n)) {
      print("forked")
      return "small"
    }
  }
  print(size)
}
