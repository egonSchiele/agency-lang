{
  "name": "agency-lang-workspace",
  "private": true,
  "scripts": {
    "build": "pnpm -r --if-present --stream run build",
    "test": "pnpm -r --if-present --stream run test",
    "test:run": "pnpm -r --if-present --stream run test:run",
    "test:agency": "pnpm -r --if-present --stream run test:agency",
    "test:agency-js": "pnpm -r --if-present --stream run test:agency-js",
    "docs": "pnpm -C packages/agency-lang/docs-new run build"
  }
}