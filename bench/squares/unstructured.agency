import { generateRandomIntegerList } from "./benchFuncs.js"

node main() {
  nums: number[] = generateRandomIntegerList(5)
  print(nums)
  printTime {
    squares = llm("What are the squares of the following numbers: ${nums}?")
    print(squares)
  }
}