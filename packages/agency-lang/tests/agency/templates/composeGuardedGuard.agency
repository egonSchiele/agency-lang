def guarded(): string {
  const result = guard(time: #minutes) {
    #body
  }
  if (isFailure(result)) {
    return "tripped"
  }
  return result.value
}

export node main(): string {
  return guarded()
}
