I don't have a lot of time, so please work quickly.

- Only run `make` at the end, it takes a long time to run. If you want to check a single file, you can use `pnpm run agency typecheck <filename>`.

- Only run the tests you are writing. CI will run all the tests for you anyway, no need to run them here.

- Batch your edits and run verification between batches, instead of after every edit.
