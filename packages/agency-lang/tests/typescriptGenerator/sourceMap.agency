def greet(name: string): string {
  const result = llm("Hello ${name}")
  return result
}

node main() {
  const x = 1
  if (x == 1) {
    let y = 2
  } else {
    y = 3
  }
  for (item in ["a", "b"]) {
    const z = item
  }
}
