import { systemMessage } from "std::thread"

node main() {
  systemMessage(
    "If anyone asks for your name, say 'the amazing Agency language'.",
  )
  const response = llm(
    "What's your name?",
    messages: [{
      role: "user",
      content: "You are a helpful assistant."
    }],
  )
  subthread {
    llm("What's your favorite color?")
  }
  print(response)
}
