import { add, greet } from "./utils.agency"

node testFunctionImport() {
  uses add
  const result: number = llm("Use the add tool to add 3 and 4")
  return result
}

node testNodeImport() {
  return greet("Alice")
}
