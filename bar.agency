def greet(name: string): string {
  return "Kya chal raha jai, ${name}!"
}

node sayHi(name) {
  +greet
  response = llm("Greet the user with their name: ${name} using the greet function.")
  print(response)
}