const log = []

node main() {
  thread {
    for (i in range(3)) {
      log.push("i:" + i)
      if (i == 1) {
        const y = interrupt("deep")
        log.push("resumed")
      }
    }
  }
  return log
}