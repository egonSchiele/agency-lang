Given the following knowledge graph, identify which entities are relevant to the query. Return a JSON array of entity names.

Knowledge graph:
{{{graphIndex:string}}}

Query: {{{query:string}}}

Return only the JSON array of entity names, e.g. ["Mom", "Dad"]. Return [] if no entities are relevant.
