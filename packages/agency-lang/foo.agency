import { diff } from "std::syntax"
const file1 = read("testfile") with approve
const file2 = read("testfile2") with approve

node main() {
  const res = diff(file1, file2, color: true)
  print(res)
}
