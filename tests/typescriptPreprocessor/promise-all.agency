def slowTask(label: string): string {
  const result = llm("Process: ${label}")
  return result
}

node main() {
  const a: string = llm("First async task")
  const b: string = llm("Second async task")
  const combined = "${a} and ${b}"
  print(combined)
}
