def compute(): Result {
  return success(42)
}

node main() {
  const r: Result! = compute()
  if (isSuccess(r)) {
    const inner = r.value
    if (isSuccess(inner)) {
      if (inner.value == 42) {
        return "double-wrapped correctly"
      }
    }
  }
  return "not double-wrapped"
}
