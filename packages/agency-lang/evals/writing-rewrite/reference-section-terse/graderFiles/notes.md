Context: a must-read section of the repository's CLAUDE.md explaining how interrupts work. The reader is an AI agent that has never seen the language and is about to change, review, or write a spec for code in this area. The facts in the text are correct; the problems are all in how it is written.

- The lifecycle steps are one-line mechanism names with file and line citations (`interruptWithHandlers` (`agencyInterrupt.ts:166`), `respondToInterruptsCore` restores the checkpoint (`interrupts.ts:774`)). A reader who does not know the runtime learns nothing from a function name and a line number. Each step should say in sentences what happens to the program, and the citations should go.
- Nothing says why any of this exists. The text states rules (every handler runs, any reject wins) without saying what they are for: every function is a tool an LLM can call, interrupts sit in front of dangerous or sensitive actions, and the all-handlers rule is what guarantees a user can always refuse. Say the purpose before the rules.
- The `env("FOO")` example is introduced and then dropped. The steps and the consequences talk about "the interrupt" in the abstract. Carry the one example through: show a handler wrapped around `requestEnvRead("FOO")`, and when explaining that the interrupt data is not used on resume, say it with that example (a caller cannot approve this interrupt and pass a different variable name).
- "Handler" is defined by listing three syntaxes and no handler is ever shown. Show one `handle { … } with (intr) { … }` block before the rules about how handlers combine.
- The decision rule is written as a formula the reader has to decode: "any reject → rejected; else ≥1 approve → approved; else, or on an explicit propagate, → surface". Write it as plain sentences: every handler in the chain runs; if any rejects, the interrupt is rejected; if none rejects and at least one approves, it is approved; if none approves, or any says propagate, it goes to a human.
- The ways a human can answer are squeezed into one parenthetical ("the CLI via `--interactive` or per-effect `--approve`/`--reject`, a TypeScript caller, or an HTTP/MCP caller of a served agent"). Each of the three deserves its own short paragraph saying what the caller sees and does, with the command line shown for the CLI case.
- "Three consequences you must not get wrong", the bold "never" sentences, and "stated" imperatives lecture the reader instead of explaining. Drop the headline, drop the bold, and let the explanation carry the point. "The four words" as a heading is cute in the same way.
- The closing map of dev docs is one paragraph of seven clauses joined by semicolons. Make it a list, one line per doc, each saying what the doc covers.
