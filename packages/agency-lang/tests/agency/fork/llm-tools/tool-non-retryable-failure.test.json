{
  "tests": [
    {
      "nodeName": "main",
      "description": "A destructive def fails, so its failure carries destructiveRan. runPrompt's isFailure path pushes a 'can no longer be called' tool message and removes the tool. The LLM should not retry the call.",
      "input": "",
      "expectedOutput": "\"tool-removed\"",
      "evaluationCriteria": [{ "type": "exact" }],
      "interruptHandlers": [],
      "llmMocks": [
        { "toolCall": { "name": "alwaysFail", "args": {} } },
        { "return": { "msg": "tool-removed" } }
      ],
      "retry": 2
    }
  ]
}
