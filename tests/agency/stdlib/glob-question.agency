import { glob } from "std::shell"

node main() {
  handle {
    const results = glob("?.md", "tests/agency/stdlib/glob-fixtures") catch []
  } with (data) {
    return approve()
  }
  return results
}
