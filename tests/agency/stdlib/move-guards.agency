import { move } from "std::fs"

node main() {
  let badSrc = move("/tmp", "/tmp/foo/bar") with approve
  let badDest = move("/tmp/foo", "/") with approve
  return {
    badSrcRefused: isFailure(badSrc),
    badDestRefused: isFailure(badDest)
  }
}
