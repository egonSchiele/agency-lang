import { writeAgency } from "std::agency"

node main() {
  const r = writeAgency("Write a program that adds up the numbers from 1 to 5 and returns the total.") with approve
  const marker = match (r) {
    success(_) => "COMPILED"
    failure(_) => "FAILED"
  }
  print(marker)
}
