{
  "tests": [
    {
      "nodeName": "main",
      "description": "Fork where one thread is approved and the other is rejected — approved thread returns value, rejected thread returns failure",
      "input": "",
      "expectedOutput": "[{\"success\":false},{\"success\":false,\"error\":\"interrupt rejected\"}]",
      "evaluationCriteria": [
        {
          "type": "exact"
        }
      ],
      "interruptHandlers": [{ "action": "approve" }, { "action": "reject" }]
    }
  ]
}
