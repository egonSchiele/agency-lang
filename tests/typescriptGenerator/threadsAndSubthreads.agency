def foo() {
  thread {
    const res1: number[] = llm("What are the first 5 prime numbers?")
    subthread {
      const res2: number[] = llm("What are the next 2 prime numbers after those?")
      subthread {
        const res3: number = llm("And what is the sum of all those numbers combined?")
      }
      thread {
        const res5: number = llm("And what is the sum of all those numbers combined?")
      }
    }
    subthread {
      const res4: number = llm("And what is the sum of all those numbers combined?")
    }
  }
  print("res1", res1)
  print("res2", res2)
  print("res3", res3)
  print("res4", res4)
  print("res5", res5)
}

node main() {
  thread {
    const res1: number[] = llm("What are the first 5 prime numbers?")
    subthread {
      const res2: number[] = llm("What are the next 2 prime numbers after those?")
      subthread {
        const res3: number = llm("And what is the sum of all those numbers combined?")
      }
      thread {
        const res5: number = llm("And what is the sum of all those numbers combined?")
      }
    }
    subthread {
      const res4: number = llm("And what is the sum of all those numbers combined?")
    }
  }
  print("res1", res1)
  print("res2", res2)
  print("res3", res3)
  print("res4", res4)
  print("res5", res5)
}