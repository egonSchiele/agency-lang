const log = []

def sideEffect(val: string) {
  sleep(50ms)
  log.push(val)
}

def compute(val: number): number {
  sleep(50ms)
  return val * 2
}

node main() {
  const x = async compute(5)
  async sideEffect("side")
  return { x: x, log: log }
}
