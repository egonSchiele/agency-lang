{
  "tests": [
    {
      "nodeName": "main",
      "skip": true,
      "description": "[SKIPPED — known bug] Handler that calls interrupt() recursively invokes itself, causing infinite recursion (RangeError). Expected: handler-issued interrupts should propagate past the current handler to the user (or to outer handlers), then the handler resumes and approves the inner interrupts.",
      "input": "",
      "expectedOutput": "[\"inner done: a\",\"inner done: b\"]",
      "evaluationCriteria": [{ "type": "exact" }],
      "interruptHandlers": [
        { "action": "approve" },
        { "action": "approve" }
      ]
    }
  ]
}
