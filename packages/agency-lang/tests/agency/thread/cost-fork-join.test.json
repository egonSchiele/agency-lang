{
  "tests": [
    {
      "nodeName": "main",
      "input": "",
      "expectedOutput": "\"true_true\"",
      "evaluationCriteria": [{ "type": "exact" }],
      "useTestLLMProvider": true,
      "llmMocks": [
        { "return": "seed" },
        { "return": "b1" },
        { "return": "b2" },
        { "return": "b3" },
        { "return": "b4" }
      ],
      "description": "Per-branch cost tracking: each fork branch is seeded with the parent's cost at creation, accumulates its own, and on join propagates its delta back to the parent. Pinned to the deterministic provider via useTestLLMProvider so the cost-per-call number is stable on push-to-main CI too."
    }
  ]
}
