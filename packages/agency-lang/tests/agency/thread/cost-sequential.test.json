{
  "tests": [
    {
      "nodeName": "main",
      "input": "",
      "expectedOutput": "\"0_true_true\"",
      "evaluationCriteria": [{ "type": "exact" }],
      "useTestLLMProvider": true,
      "llmMocks": [
        { "return": "first response" },
        { "return": "second response" }
      ],
      "description": "getCost() grows by the deterministic provider's SYNTHETIC_COST after each llm() call. Pins itself to the deterministic provider via useTestLLMProvider so the cost-per-call number is stable on push-to-main CI too."
    }
  ]
}
