{
  "tests": [
    {
      "nodeName": "main",
      "description": "an outer block's variable passed from a nested block into a mutating function is mutated in place",
      "input": "",
      "expectedOutput": "3",
      "evaluationCriteria": [{ "type": "exact" }]
    }
  ]
}
