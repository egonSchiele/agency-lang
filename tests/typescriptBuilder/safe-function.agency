import { safe lookupItem, saveItem } from "./tools.js"

safe def safeLookup(id: string) {
  return lookupItem(id)
}

def unsafeSave(id: string) {
  saveItem(id)
  return lookupItem(id)
}

node main() {
  const result = llm("Use the tools", { tools: [safeLookup, unsafeSave] })
  return result
}
