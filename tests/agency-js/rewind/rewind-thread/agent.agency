node main(message: string) {
  thread {
    mood: "happy" | "sad" = llm("Categorize the mood: ${message}")
    response: string = llm("Respond with just this word, and an excalamation mark at the end: ${mood}")
  }
  return { 
    mood: mood,
    response: response
  }
}