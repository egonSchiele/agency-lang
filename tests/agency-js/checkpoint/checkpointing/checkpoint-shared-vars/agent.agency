shared let sharedCounter = 0
let counter = 0

node main() {
  const cp = checkpoint()
  sharedCounter = sharedCounter + 1
  counter = counter + 1
  if (sharedCounter < 3) {
    restore(cp, {})
  }
  return { sharedCounter: sharedCounter, counter: counter }
}
