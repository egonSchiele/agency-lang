let globalVar = "unchanged"

node foo(sec: number, newGlobalVar: string | null) {
  if (newGlobalVar) {
    globalVar = newGlobalVar
  }
  const confirmation = interrupt("confirm")
  sleep(sec)
  return globalVar
}
