def getWeather(city: string): string {
  return fetch("https://weather.example.com/${city}")
}

def formatTemp(temp: number): string {
  return "${temp} degrees"
}

node main() {
  uses getWeather, formatTemp
  const forecast: string = llm("What is the weather in NYC?")  const summary: string = llm("Summarize the forecast")
}