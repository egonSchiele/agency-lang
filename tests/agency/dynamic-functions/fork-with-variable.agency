def double(x: number): number {
  return x * 2
}

def triple(x: number): number {
  return x * 3
}

node main() {
  const fns = [double, triple]
  let results: number[] = []
  fork(fns) as fn {
    results.push(fn(5))
  }
  return results
}
