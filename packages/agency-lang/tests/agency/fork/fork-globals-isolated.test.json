{
  "tests": [
    {
      "nodeName": "main",
      "description": "Each fork branch sees its own snapshot of globals; writes never leak back to the parent (per-branch isolation).",
      "input": "",
      "expectedOutput": "[0,[1,1,1]]",
      "evaluationCriteria": [{ "type": "exact" }]
    }
  ]
}
