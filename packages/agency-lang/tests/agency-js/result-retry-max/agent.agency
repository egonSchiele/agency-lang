def alwaysFails(input: string): Result {
  return failure("nope")
}

node main() {
  let result = alwaysFails("hello")
  if (isFailure(result)) {
    restore(result.checkpoint, { args: { input: "hello" } })
  }
  return result.value
}
