def pureFunction(x) {
  return x
}

def llmFunction(prompt) {
  result = llm("Respond to: ${prompt}")
  return result
}

node main() {
  a = pureFunction("hello")
  b = llmFunction("world")
  result: string = llm("Combine ${a} and ${b}")
  print(result)
}
