import { highlight } from "std::syntax"

node main() {
  const file = read("README.md") with approve
  if (file is success(data)) {
    print(highlight(data, "markdown"))
  } else {
    print("Failed to read file: ${file.error}")
  }
}
