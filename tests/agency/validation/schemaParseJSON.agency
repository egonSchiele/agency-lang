node main() {
  const s = schema(number)
  const good = s.parseJSON("42")
  const bad = s.parseJSON("not json")
  if (isSuccess(good)) {
    if (isFailure(bad)) {
      return good.value
    }
  }
  return "failed"
}
