{
  "name": "agency-lang-workspace",
  "private": true,
  "scripts": {
    "build": "pnpm -C packages/agency-lang run build",
    "test": "pnpm -C packages/agency-lang run test",
    "test:run": "pnpm -C packages/agency-lang run test:run"
  }
}
