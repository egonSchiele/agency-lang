import { isEmail } from "std::validation"

@validate(isEmail)
type Email = string

node main() {
  const good: Email! = "user@example.com"
  if (isSuccess(good)) {
    return "ok"
  }
  return "failed"
}
