shared let log = ""

node main() {
  const cp = checkpoint()
  log = log + "start,"
  let x = 0
  while (x < 3) {
    log = log + x
    x = x + 1
  }
  if (log == "start,012") {
    restore(cp, {})
  }
  return log
}