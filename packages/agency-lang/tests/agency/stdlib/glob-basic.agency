import { glob } from "std::shell"
import { sortBy } from "std::array"

node main() {
  const results = glob("**/*.{md,ts}", __dirname + "/glob-fixtures") catch [] with approve
  let sorted = sortBy(results) as r {
    return r
  }
  return sorted
}
