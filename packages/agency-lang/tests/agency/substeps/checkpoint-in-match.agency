shared let log = ""

node main(action: string) {
  const cp = checkpoint()
  log = log + "before,"
  match(action) {
    "go" => log = log + "go,"
    _ => log = log + "other,"
  }
  if (log == "before,go,") {
    restore(cp, {})
  }
  return log
}