import { agencyCodingAgent } from "std::agents/agency/coding"

node main() {
  const r = agencyCodingAgent("Write a program that adds up the numbers from 1 to 5 and returns the total.") with approve
  const marker = if isFailure(r) then "FAILED" else "COMPILED"
  print(marker)
}
