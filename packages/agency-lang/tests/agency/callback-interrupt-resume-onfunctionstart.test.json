{
  "tests": [
    {
      "nodeName": "main",
      "description": "callback raising an interrupt from onFunctionStart halts the surrounding function, propagates up, and resumes after a user approve response. calls == 1 because the callback's own checkpoint preserves the callback frame's substep counters — on resume the increment step is already past, so only the interrupt step re-runs (and finds the user's response).",
      "input": "",
      "expectedOutput": "[1,\"ok\"]",
      "evaluationCriteria": [
        {
          "type": "exact"
        }
      ],
      "interruptHandlers": [
        {
          "action": "approve",
          "expectedMessage": "pause"
        }
      ]
    }
  ]
}
