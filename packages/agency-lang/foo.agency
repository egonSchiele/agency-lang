import { colorSchemes, highlight } from "std::syntax"
const data = read("foo.agency") with approve

node main(): string {

  for (theme in colorSchemes) {
    print(theme)
    print("------------------")
    print(highlight(data, theme: theme, language: "ts"))
    print("\n\n")
  }
}
