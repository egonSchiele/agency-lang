node main() {
  const numbers: number[] = llm("the first 5 prime numbers")
  print(numbers)

  const greetings: string[] = llm("a list of 3 common greetings in different languages")
  print(greetings)
}