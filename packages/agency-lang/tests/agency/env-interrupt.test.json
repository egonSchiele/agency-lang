{
  "tests": [
    {
      "nodeName": "main",
      "description": "Reading an environment variable raises std::env, and an approved read returns the real value",
      "input": "",
      "expectedOutput": "\"std::setEnv std::env readBack=hello\"",
      "evaluationCriteria": [{ "type": "exact" }]
    },
    {
      "nodeName": "rejected",
      "description": "A rejected std::env blocks the read even though the variable is set",
      "input": "",
      "expectedOutput": "\"readBack=blocked\"",
      "evaluationCriteria": [{ "type": "exact" }]
    }
  ]
}
