node main() {
  foo: number[] = llm("What are some numbers?", { 
    maxTokens: 10,
    messages: [{ 
      role: "system",
      content: "You are a helpful assistant that generates numbers."
    }]
  })
  print(foo[0])
}