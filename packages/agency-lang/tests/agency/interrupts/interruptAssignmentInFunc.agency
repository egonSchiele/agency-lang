def greet(): string {
  const name = interrupt("Who do you want to say hi to?")
  return "Kya chal raha jai, ${name}!"
}

node foo() {
  const greeting = greet()
  return { 
    greeting: greeting
  }
}