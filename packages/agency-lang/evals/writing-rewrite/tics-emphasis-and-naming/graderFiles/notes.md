Context: a rubric an LLM judge applies. Plain directives, lowercase, no names for the rules.

- "A quick word on how this standard is organized: first the example, then the rule, then the checks." announces the structure instead of using it. Delete it.
- "puts a fence around the block and hands back a Result" writes about the code as if it were a person with a clever image. Say what it does: limits the time of the block and returns a Result.
- "ONLY", "NEVER", and "OWN" in capitals shout at the reader. Write them in lowercase; the check itself carries the rule.
- "No shared budgets, no outer guards, no exceptions." is a list of prohibitions used for rhythm. Check 1 already says one guard per call; delete the list.
- "The "one document, one fence" principle" gives the rule a cute name. Delete it; the checks state the rule.
