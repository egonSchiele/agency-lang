import { glob } from "std::shell"

node main() {
  let result = glob("{ts", __dirname + "/glob-fixtures") with approve
  let hasError = false
  if (isFailure(result)) {
    hasError = result.error != null
  }
  return {
    ok: isSuccess(result),
    hasError: hasError
  }
}
