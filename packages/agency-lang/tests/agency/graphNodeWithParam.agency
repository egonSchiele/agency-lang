type User = {
  name: string;
  age: number
}

node foo(user: User) {
  const name = user.name
  const greeting = llm("Say hello to ${user.name}")
  return {
    name: name,
    greeting: greeting
  }
}

node main() {
  const user: User = {
    name: "Alice",
    age: 30
  }
  return foo(user)
}