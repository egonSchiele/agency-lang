import tool { addTask, listTasks, markTaskDone } from "./subagents/task.agency"

systemPrompt = read("./prompts/system.md")
node main() {
  thread {
    system(systemPrompt)
    greeting: string = llm("Say hello world and introduce yourself as the Agency language assistant.")
    print(greeting)
  }
}