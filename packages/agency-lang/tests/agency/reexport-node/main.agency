// Consumer file: imports re-exported symbols FROM the re-exporter (lib.agency),
// not from source. Exercises the JS-level re-export of `__<name>NodeParams`.
import node { srcNode } from "./lib.agency"
import { srcFn, renamedSafe } from "./lib.agency"

node callNode() {
  return srcNode(21)
}

node callFn() {
  return srcFn(7)
}

node callRenamedSafe() {
  return renamedSafe(5)
}
