/*
this.threads[id] = this.threads[parentId].newSubthreadChild();
^

TypeError: Cannot read properties of undefined (reading 'newSubthreadChild')
*/
node main() {
  // no parent thread,
  // should still create a thread
  msgs = subthread {
    res1: number[] = llm("What is the first prime number > 100?")
    res2: number[] = llm("What are the next 2 prime numbers after that?")
  }

  return { 
    res1: res1,
    res2: res2,
    msgs: msgs
  }
}