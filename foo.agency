import node { sayHi } from "./bar.agency"
import tool { greet } from "./bar.agency"

node main() {
  name = input("> ")
  fibs: number[] = llm("Generate a list of first 10 Fibonacci numbers.")
  print("First 10 Fibonacci numbers: ${fibs}")
  return sayHi(name)
}