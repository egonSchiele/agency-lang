node main() {
  numbers :: number[]
  numbers = llm("the first 5 prime numbers")
  print(numbers)

  greetings :: string[]
  greetings = llm("a list of 3 common greetings in different languages")
  print(greetings)
}