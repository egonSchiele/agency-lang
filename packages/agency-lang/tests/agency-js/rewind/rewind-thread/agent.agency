node main(message: string) {
  thread {
    const mood: "happy" | "sad" = llm("Categorize the mood: ${message}")
    const response: string = llm("Respond with just this word, and an excalamation mark at the end: ${mood}")
  }
  return { 
    mood: mood,
    response: response
  }
}