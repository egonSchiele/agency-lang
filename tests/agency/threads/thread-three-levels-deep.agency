node main() {
  msgs = thread {
    res1: number[] = llm("What is the first prime number > 100?")
    msgs2 = thread {
      res2: number[] = llm("What are the next 2 prime numbers after that?")
      msgs3 = subthread {
        res3: number[] = llm("What are the next 2 prime numbers after that?")
      }
      msgs4 = thread {
        res4: number[] = llm("What are the next 2 prime numbers after that?")
      }

    }
    msgs5 = subthread {
      res5: number[] = llm("What are the next 2 prime numbers after that?")
      msgs6 = subthread {
        res6: number[] = llm("What are the next 2 prime numbers after that?")
      }
      msgs7 = thread {
        res7: number[] = llm("What are the next 2 prime numbers after that?")
      }

    }
  }

  return { 
    msgs: msgs,
    msgs2: msgs2,
    msgs3: msgs3,
    msgs4: msgs4,
    msgs5: msgs5,
    msgs6: msgs6,
    msgs7: msgs7
  }
}