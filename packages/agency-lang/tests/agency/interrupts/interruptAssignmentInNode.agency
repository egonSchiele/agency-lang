type User = {
  name: string;
  age: number
}

const user: User = { 
  name: "Alice",
  age: 30
}

node foo() {
  const newName = interrupt("What's the new name?")
  user.name = newName
  return user
}