node foo() {
  const msgs = thread {
    const res1: string = llm("My favorite number is 3435.")
  }
  return msgs
}

node bar(msgs) {
  const res2: number = llm("What is my favorite number? Reply with just the number, nothing else.", {
    messages: msgs
  })
  return res2
}
