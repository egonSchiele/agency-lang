def slowTask(label: string): string {
  result = llm("Process: ${label}")
  return result
}

node main() {
  a: string = llm("First async task")
  b: string = llm("Second async task")
  combined = "${a} and ${b}"
  print(combined)
}
