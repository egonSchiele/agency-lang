node main() {
  const items = ["a", "b", "c", "d"]
  let result = ""
  for (item in items) {
    if (item == "c") {
      const y = interrupt("found c")
    }
    result = result + item
  }
  return result
}