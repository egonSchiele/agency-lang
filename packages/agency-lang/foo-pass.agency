import { researcherAgent } from "./researcher-live.agency"

node main() {
  handle {
    const prompt = "What are the top news stories today?"
    const result = researcherAgent(task: prompt)
    print(result)
    return result
  } with (data) {
    return match(data.effect) {
      _ => pass()
    }
  }
}
