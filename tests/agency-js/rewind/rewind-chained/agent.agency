node main(message: string) {
  const mood: "happy" | "sad" = llm("Categorize the mood: ${message}")
  const confidence: "high" | "low" = llm("How confident are you? ${mood}")
  return { mood: mood, confidence: confidence }
}
