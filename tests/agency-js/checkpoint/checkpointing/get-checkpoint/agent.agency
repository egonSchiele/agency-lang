shared let sharedVar = 0
let globalVar = 0

def increment(amount: number) {
  sharedVar = sharedVar + amount
  globalVar = globalVar + amount
  return amount
}

def modifyState(amount1: number) {
  const localVar = 10
  const total = amount1 + localVar
  const result = increment(total)
  const cid = checkpoint()
  return cid
}

node main() {
  const cid = modifyState(3)
  return getCheckpoint(cid)
}