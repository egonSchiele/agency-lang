const adjective = "hot"
node main(name: string, item: string) {
  const adjective2 = "spicy"
  const result: string = llm("Respond with ONLY this exact text: '${name} wants ${adjective} ${adjective2} ${item}'")
  return result
}