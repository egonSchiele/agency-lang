def transform(val: string): string {
  sleep(50ms)
  return "transformed: ${val}"
}

def helper(val: string): string {
  const x = async transform(val)
  return x
}

node main() {
  const a = async helper("first")
  const b = async helper("second")
  return [a, b]
}
