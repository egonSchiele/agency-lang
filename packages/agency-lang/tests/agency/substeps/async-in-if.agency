def double(val: number): number {
  sleep(50ms)
  return val * 2
}

node main(x: number) {
  if (x > 5) {
    const a = async double(x)
    const b = async double(x + 1)
    let result = [a, b]
  } else {
    result = [0, 0]
  }
  return result
}