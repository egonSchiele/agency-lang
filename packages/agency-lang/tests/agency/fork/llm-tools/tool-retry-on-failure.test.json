{
  "tests": [
    {
      "nodeName": "main",
      "description": "Tool returns a retryable failure on bad input. The LLM should retry with corrected input after seeing the 'Error: ... You may retry' tool message. Tests runPrompt's currently-uncovered retry path.",
      "input": "",
      "expectedOutput": "25",
      "evaluationCriteria": [{ "type": "exact" }],
      "interruptHandlers": [],
      "llmMocks": [
        { "toolCall": { "name": "divideBy", "args": { "divisor": 0 } } },
        { "toolCall": { "name": "divideBy", "args": { "divisor": 4 } } },
        { "return": { "value": 25 } }
      ],
      "retry": 2
    }
  ]
}
