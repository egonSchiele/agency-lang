import { firstValid } from "std::firstValid"

def isGreaterThanFive(n: number): boolean {
  return n > 5
}

def double(n: number): number {
  return n * 2
}

node main() {
  let result = firstValid([1, 3, 4, 7], isGreaterThanFive) as v {
    return double(v)
  }
  return result
}
