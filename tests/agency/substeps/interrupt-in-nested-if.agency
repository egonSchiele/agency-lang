const log = []

node main(x: number, y: number) {
  log.push("start")
  if (x > 5) {
    log.push("outer")
    if (y > 3) {
      log.push("inner")
      return interrupt("check")
      log.push("confirmed inner")
    }
    log.push("done")
  }
  return log
}