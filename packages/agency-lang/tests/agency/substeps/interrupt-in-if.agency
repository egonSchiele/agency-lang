const log = []

node main(x: number) {
  log.push("start")
  if (x > 5) {
    log.push("big")
    return interrupt("check")
    log.push("confirmed big")
  } else {
    log.push("small")
  }
  return log
}