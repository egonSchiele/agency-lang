const log = []

node main(x: number) {
  if (x > 5) {
    for (i in range(4)) {
      if (i == 2) {
        const y = interrupt("loop interrupt")
      }
      log.push(i)
    }
    log.push("done")
  } else {
    log.push("small")
  }
  return log
}