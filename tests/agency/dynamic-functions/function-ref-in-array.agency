def double(x: number): number {
  return x * 2
}

def triple(x: number): number {
  return x * 3
}

node main() {
  const fns = [double, triple]
  const fn1 = fns[0]
  const fn2 = fns[1]
  const r1 = fn1(5)
  const r2 = fn2(5)
  return { r1: r1, r2: r2 }
}
