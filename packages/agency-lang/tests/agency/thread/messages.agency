import { systemMessage, userMessage, assistantMessage } from "std::thread"

node main() {
  systemMessage("system context")
  userMessage("user said something earlier")
  assistantMessage("assistant replied")
  return "ok"
}
