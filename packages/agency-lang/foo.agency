import { greet } from "./bar.agency"
import { map } from "std::array"
type Foo = {
  name: string;
  description: string;
  capabilities: string[]
}

export def main(countryName: string) {
  const response = llm("What is the capital of ${countryName}?")
  return response
}