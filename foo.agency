type Category = "reminder" | "todo" | "general" | "quit"

node categorize(message: string) {
  category: Category = llm("${prompt}. Here's the user message: '${message}'")
  return category
}

node main() {
  userMessage = "Remind me to buy milk tomorrow"
  return categorize(userMessage)
}