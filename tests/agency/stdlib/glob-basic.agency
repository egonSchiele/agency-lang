import { glob } from "std::shell"

node main() {
  handle {
    const results = glob("**/*.{md,ts}", "tests/agency/stdlib/glob-fixtures")
  } with (data) {
    return approve()
  }
  let sorted = sortBy(results) as r {
    return r
  }
  return sorted
}
