
node main() {
  const result = guard(cost: 0.000001) {
    const reply = llm("Reply with: pong")
    return reply
  }
  if (isFailure(result)) { return "failed:${result.data.type}" }
  return "no trip"
}
