{
  "tests": [
    {
      "nodeName": "callNode",
      "description": "Re-exported node is invoked locally and returns its result (no SimpleMachine collision)",
      "input": "",
      "expectedOutput": "42",
      "evaluationCriteria": [{ "type": "exact" }]
    },
    {
      "nodeName": "callFn",
      "description": "Re-exported function is invoked through the synthesized wrapper and returns its result",
      "input": "",
      "expectedOutput": "107",
      "evaluationCriteria": [{ "type": "exact" }]
    },
    {
      "nodeName": "callRenamedSafe",
      "description": "Aliased function re-export resolves under its local alias and is callable. NOTE: this test does not verify the `safe` flag at runtime — it only proves the renamed wrapper compiles and dispatches correctly.",
      "input": "",
      "expectedOutput": "50",
      "evaluationCriteria": [{ "type": "exact" }]
    }
  ]
}
