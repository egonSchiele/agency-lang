import { readFile } from "./basicFunctions.ts"
import node { categorize } from "./bar.agency"

def readFileTool(path: string): string {
  return await readFile(path)
}

node bar(msg) {
  print("This is the bar node.")
}

node main() {
  msg = input("> ")
  //return bar(msg)
  return categorize(msg)

  // +readFileTool
  // response = llm("Help me read the specified file: ${msg}")
  // printJSON(response)
}