{
  "tests": [
    {
      "nodeName": "main",
      "description": "run() returns failure when subprocess throws a runtime error",
      "input": "",
      "expectedOutput": "\"caught error\"",
      "evaluationCriteria": [{ "type": "exact" }]
    }
  ]
}
