{
  "tests": [
    {
      "nodeName": "main",
      "description": "Two-arm parallel where one arm interrupts and one runs to completion. Under per-branch isolation the parent's global counter stays at 0 because branch writes never leak; the test now verifies the cached arm's return value (`n1`) survives the resume cycle.",
      "input": "",
      "expectedOutput": "\"n1,ok,counter:0\"",
      "evaluationCriteria": [{ "type": "exact" }],
      "interruptHandlers": [
        { "action": "approve" }
      ]
    }
  ]
}
