def foo() {
  return interrupt("check")
  return "after interrupt"
}

node main() {
  handle {
    const result = foo()
  } with (data) {
    return reject("not allowed")
  }
  if (isFailure(result)) {
    return result.error
  }
  return result
}